# ============================
# Dockerfile.dev - React + Vite + TS
# ============================

FROM node:20-alpine

# 1️⃣ Dossier de travail
WORKDIR /app

# 2️⃣ Copier uniquement les fichiers de dépendances
COPY package*.json ./

# 3️⃣ Installer les dépendances
RUN npm install

# 4️⃣ Copier le reste du projet
COPY . .

# 5️⃣ Exposer le port de Vite
EXPOSE 5173

# 6️⃣ Démarrer Vite sur toutes les interfaces réseau
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
