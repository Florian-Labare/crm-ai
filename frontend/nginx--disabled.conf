server {
    listen 80;
    server_name _;

    # Fichiers statiques (build Vite)
    root /usr/share/nginx/html;
    index index.html;

    # Healthcheck
    location /health {
        return 200 "OK";
        add_header Content-Type text/plain;
    }

    # Si vous avez un SPA (React Router), on renvoie vers index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # (Optionnel) Proxy API local si vous voulez éviter CORS en prod
    # location /api/ {
    #     proxy_pass http://host.docker.internal:8000;  # backend Laravel en local
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
}
